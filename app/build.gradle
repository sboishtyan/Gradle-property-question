plugins {
    id 'java'
}

logger.quiet("Property `a` value by `providers.gradleProperty`: ${providers.gradleProperty("a").getOrNull()}")
logger.quiet("Property `a` value by `property`: ${property("a")}")